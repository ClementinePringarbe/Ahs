{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}
    <h1>Episode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ episode.name }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ episode.resume }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ episode.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_episode_index') }}">back to list</a>

    <a href="{{ path('app_back_episode_edit', {'id': episode.id}) }}">edit</a>

    {{ include('back/episode/_delete_form.html.twig') }}
{% endblock %}
